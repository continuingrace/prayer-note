@import url('https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css');
@import url('https://fonts.googleapis.com/css2?family=Wanted+Sans:wght@300;400;500;600;700&display=swap');

:root {
  --bg:        #F8F6F2;
  --bg-card:   #FFFFFF;
  --bg-input:  #F4F1EC;
  --bg-tag:    #EEE9E2;
  --border:    #E5DFD7;
  --border-2:  #D4CEC6;
  --ink-1:     #1E1A17;
  --ink-2:     #6B6059;
  --ink-3:     #A89E95;
  --green:     #4E7A5C;
  --green-bg:  #EBF2ED;
  --red:       #9E3E30;
  --accent:    #2D2520;
  --radius:    14px;
  --radius-sm: 9px;
  --font:      'Pretendard', 'Apple SD Gothic Neo', sans-serif;
  --fontb:     'Wanted Sans', 'Pretendard', sans-serif;
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html, body { height: 100%; overflow: hidden; background: var(--bg); }
body { font-family: var(--font); color: var(--ink-1); -webkit-font-smoothing: antialiased; }
input, textarea, select, button { font-family: var(--font); }
button { cursor: pointer; border: none; background: none; }
::placeholder { color: var(--ink-3); }

/* SCREENS */
.screen { position: fixed; inset: 0; opacity: 0; pointer-events: none; transition: opacity 0.3s; z-index: 1; }
.screen.active { opacity: 1; pointer-events: all; z-index: 2; }

/* ── COVER ── */
#screen-cover {
  background: var(--bg);
  display: flex; align-items: center; justify-content: center;
}
.cover-inner {
  display: flex; flex-direction: column; align-items: center;
  gap: 36px; padding: 48px 36px; text-align: center;
}

/* 폴더 SVG — 클립 없이 깔끔하게 */
.cover-folder-svg {
  width: 130px; height: auto;
  filter: drop-shadow(0 6px 16px rgba(45,37,32,0.10));
  animation: float 5s ease-in-out infinite;
}
@keyframes float { 0%,100%{transform:translateY(0)} 50%{transform:translateY(-7px)} }

.cover-eyebrow {
  font-family: var(--fontb);
  font-size: 11px; font-weight: 600;
  letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-3); margin-bottom: 6px;
}
.cover-title {
  font-family: var(--fontb);
  font-size: 30px; font-weight: 800;
  color: var(--ink-1); letter-spacing: -0.03em;
  line-height: 1.2; margin-top: 2px;
}
.cover-desc {
  font-size: 14px; font-weight: 400;
  color: var(--ink-2); line-height: 1.75;
  margin-top: 8px;
}
.cover-btn {
  display: inline-flex; align-items: center; gap: 8px;
  padding: 14px 34px;
  background: var(--accent); color: #FBF9F6;
  border-radius: 100px;
  font-family: var(--fontb); font-size: 14px; font-weight: 600;
  letter-spacing: 0.01em;
  box-shadow: 0 2px 12px rgba(45,37,32,0.18);
  transition: transform 0.15s, opacity 0.15s;
}
.cover-btn:active { transform: scale(0.97); opacity: 0.9; }

/* ── APP SHELL ── */
#screen-app { display: flex; flex-direction: column; overflow: hidden; background: var(--bg); }

.app-header {
  height: 52px; display: flex; align-items: center; justify-content: space-between;
  padding: 0 14px;
  background: var(--bg-card);
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}
.header-left { display: flex; align-items: center; gap: 4px; }
.header-title {
  font-family: var(--fontb); font-size: 17px; font-weight: 700;
  color: var(--ink-1); letter-spacing: -0.02em;
}
.header-right { display: flex; gap: 2px; }
.icon-btn {
  width: 38px; height: 38px; display: flex; align-items: center; justify-content: center;
  border-radius: var(--radius-sm); color: var(--ink-2); transition: background 0.15s;
}
.icon-btn:active { background: var(--bg-tag); }

/* TABS */
.tab-bar {
  display: flex; background: var(--bg-card);
  border-bottom: 1px solid var(--border); flex-shrink: 0;
}
.tab-btn {
  flex: 1; display: flex; flex-direction: column;
  align-items: center; justify-content: center; gap: 4px;
  height: 54px; color: var(--ink-3);
  font-size: 10px; font-weight: 600; letter-spacing: 0.04em;
  position: relative; transition: color 0.2s;
}
.tab-btn.active { color: var(--ink-1); }
.tab-btn.active::after {
  content: ''; position: absolute; bottom: 0; left: 18%; right: 18%;
  height: 2px; border-radius: 2px 2px 0 0; background: var(--ink-1);
}

/* TAB CONTENT */
.tab-content { display: none; flex: 1; overflow-y: auto; -webkit-overflow-scrolling: touch; }
.tab-content.active { display: flex; flex-direction: column; }

/* CATEGORY */
.category-filter {
  display: flex; gap: 6px; padding: 11px 14px;
  overflow-x: auto; flex-shrink: 0; scrollbar-width: none;
  background: var(--bg-card); border-bottom: 1px solid var(--border);
}
.category-filter::-webkit-scrollbar { display: none; }
.cat-chip {
  flex-shrink: 0; padding: 5px 14px; border-radius: 100px;
  border: 1px solid var(--border); color: var(--ink-2);
  font-size: 12px; font-weight: 500; background: transparent;
  transition: all 0.15s; white-space: nowrap;
}
.cat-chip.active { background: var(--ink-1); border-color: var(--ink-1); color: #FBF9F6; }

/* PRAYER LIST */
.prayer-list { flex: 1; padding: 14px 14px 40px; display: flex; flex-direction: column; gap: 10px; }
.empty-state {
  flex: 1; display: flex; flex-direction: column; align-items: center;
  justify-content: center; gap: 8px; padding: 60px 0; text-align: center;
}
.empty-icon { font-size: 36px; opacity: 0.4; margin-bottom: 4px; }
.empty-state p { font-size: 15px; font-weight: 500; color: var(--ink-2); }
.empty-state span { font-size: 13px; color: var(--ink-3); }

/* PRAYER CARD */
.prayer-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 18px 18px 16px;
  cursor: pointer;
  transition: border-color 0.15s;
}
.prayer-card:active { border-color: var(--border-2); }

.prayer-card-top {
  display: flex; align-items: flex-start;
  justify-content: space-between; gap: 10px; margin-bottom: 8px;
}
.prayer-card-title {
  font-family: var(--fontb);
  font-size: 15px; font-weight: 600;
  color: var(--ink-1); line-height: 1.5; flex: 1;
  letter-spacing: -0.01em; margin-bottom: 2px;
}
.prayer-cat-badge {
  flex-shrink: 0; padding: 3px 9px; border-radius: 100px;
  font-size: 10px; font-weight: 600; letter-spacing: 0.03em;
  background: var(--bg-tag); color: var(--ink-2);
  border: 1px solid var(--border);
}
.prayer-card-content {
  font-size: 13px; font-weight: 400; color: var(--ink-2);
  line-height: 1.85;
  display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical;
  overflow: hidden; margin-bottom: 14px; margin-top: 8px;
}
.prayer-card-footer {
  display: flex; align-items: center; justify-content: space-between;
  padding-top: 12px; border-top: 1px solid var(--border); margin-top: 2px;
}
.prayer-card-footer-right { display: flex; align-items: center; gap: 7px; }
.prayer-date { font-size: 11px; color: var(--ink-3); font-weight: 500; }
.prayer-status {
  font-size: 10px; font-weight: 600; padding: 3px 9px;
  border-radius: 100px; letter-spacing: 0.04em;
}
.prayer-status.ongoing { color: var(--ink-2); background: var(--bg-tag); }
.prayer-status.answered { color: var(--green); background: var(--green-bg); }
.prayer-status.paused { color: var(--ink-3); background: var(--bg-tag); }
.prayer-scripture-line {
  font-size: 12px; color: var(--green); margin-top: 12px;
  padding-top: 12px; border-top: 1px solid var(--border);
  line-height: 1.6;
}
.prayer-log-badge { font-size: 11px; color: var(--ink-3); margin-top: 7px; }

/* CALENDAR */
.calendar-wrap { padding: 16px; display: flex; flex-direction: column; gap: 16px; }
.calendar-header { display: flex; align-items: center; justify-content: space-between; }
.cal-month-label {
  font-family: var(--fontb); font-size: 16px; font-weight: 700;
  color: var(--ink-1); letter-spacing: -0.02em;
}
.cal-nav {
  width: 34px; height: 34px; display: flex; align-items: center; justify-content: center;
  border-radius: var(--radius-sm); font-size: 20px; color: var(--ink-2); transition: background 0.15s;
}
.cal-nav:active { background: var(--bg-tag); }
.calendar-grid { display: grid; grid-template-columns: repeat(7,1fr); gap: 3px; }
.cal-dow {
  text-align: center; font-size: 10px; font-weight: 700;
  color: var(--ink-3); padding: 4px 0; letter-spacing: 0.04em;
}
.cal-day {
  aspect-ratio: 1; display: flex; align-items: center; justify-content: center;
  border-radius: 50%; font-size: 13px; color: var(--ink-2);
  cursor: pointer; position: relative; transition: background 0.12s;
  font-weight: 400;
}
.cal-day:active { background: var(--bg-tag); }
.cal-day.today { font-weight: 700; color: var(--ink-1); }
.cal-day.today::before {
  content:''; position:absolute; inset:2px;
  border-radius:50%; border:1.5px solid var(--border-2);
}
.cal-day.has-prayer::after {
  content:''; position:absolute; bottom:3px;
  width:3px; height:3px; border-radius:50%; background:var(--ink-1);
}
.cal-day.selected { background:var(--ink-1); color:#FBF9F6; font-weight:600; }
.cal-day.selected::before { display:none; }
.cal-day.other-month { opacity:0.18; }
.cal-day-detail {
  background: var(--bg-card); border: 1px solid var(--border);
  border-radius: var(--radius); padding: 14px; min-height: 72px;
}
.cal-detail-hint { font-size: 13px; color: var(--ink-3); text-align: center; padding: 8px 0; }
.cal-detail-date { font-size: 12px; font-weight: 600; color: var(--ink-2); margin-bottom: 12px; letter-spacing: 0.02em; }
.cal-detail-item { padding: 10px 0; border-bottom: 1px solid var(--border); cursor: pointer; }
.cal-detail-item:last-child { border-bottom: none; }
.cal-detail-item-title { font-size: 14px; font-weight: 600; color: var(--ink-1); margin-bottom: 3px; line-height: 1.4; }
.cal-detail-item-content { font-size: 12px; color: var(--ink-2); line-height: 1.6; }

/* MODAL */
.modal-overlay {
  position: fixed; inset: 0; z-index: 100;
  background: rgba(30,26,23,0.3); backdrop-filter: blur(3px);
  display: flex; align-items: flex-end;
}
.modal-overlay.hidden { display: none; }
.modal-sheet {
  width: 100%; max-width: 640px; margin: 0 auto;
  background: var(--bg-card);
  border-radius: 20px 20px 0 0;
  border-top: 1px solid var(--border);
  max-height: 94dvh;
  display: flex; flex-direction: column;
  animation: sup 0.28s cubic-bezier(0.32,0.72,0,1);
}
@keyframes sup { from{transform:translateY(28px);opacity:0} to{transform:translateY(0);opacity:1} }
.modal-handle { width:32px; height:3px; background:var(--border); border-radius:2px; margin:10px auto 0; flex-shrink:0; }
.modal-header {
  display:flex; align-items:center; justify-content:space-between;
  padding:14px 18px 13px; border-bottom:1px solid var(--border); flex-shrink:0;
}
.modal-back { display:flex; align-items:center; gap:5px; color:var(--ink-2); font-size:13px; font-weight:500; }
.modal-action-btn {
  background:var(--accent); color:#FBF9F6;
  border-radius:var(--radius-sm); padding:8px 18px;
  font-family:var(--fontb); font-size:13px; font-weight:600;
}
.modal-action-btn:active { opacity:0.85; }
.modal-body {
  overflow-y:auto; flex:1; padding:20px 18px;
  display:flex; flex-direction:column; gap:16px;
  -webkit-overflow-scrolling:touch;
}

/* FIELDS */
.field-title {
  font-family:var(--fontb); font-size:19px; font-weight:700;
  color:var(--ink-1); letter-spacing:-0.02em; line-height:1.3;
  background:transparent; border:none; outline:none; width:100%;
  padding:2px 0 12px; border-bottom:2px solid var(--border);
}
.field-title:focus { border-bottom-color:var(--ink-1); }
.field-row.two-col { display:flex; gap:8px; }
.field-select {
  flex:1; background:var(--bg-input); border:1px solid var(--border);
  border-radius:var(--radius-sm); padding:10px 12px;
  color:var(--ink-1); font-size:13px; font-weight:500; outline:none;
  appearance:none; -webkit-appearance:none;
}
.field-section {
  background:var(--bg-input); border:1px solid var(--border);
  border-radius:var(--radius); padding:14px 14px 12px;
  display:flex; flex-direction:column; gap:12px;
}
.section-label {
  display:flex; align-items:center; justify-content:space-between;
  font-size:10px; font-weight:700; color:var(--ink-3);
  letter-spacing:0.08em; text-transform:uppercase;
}
.section-label-row { display:flex; align-items:center; justify-content:space-between; margin-bottom:10px; }
.section-label-text { font-size:12px; font-weight:700; color:var(--ink-2); letter-spacing:0.04em; }
.field-date-inline {
  background:transparent; border:none; outline:none;
  color:var(--ink-2); font-size:12px; font-weight:500; font-family:var(--font);
}
.field-date-inline::-webkit-calendar-picker-indicator { opacity:0.4; cursor:pointer; }
.field-textarea {
  background:transparent; border:none; outline:none;
  color:var(--ink-1); font-size:14px; font-weight:400;
  line-height:1.85; resize:none; width:100%; min-height:100px;
  font-family:var(--font);
}
.field-textarea.sm { min-height:70px; font-size:13px; line-height:1.75; }
.scripture-row {
  display:flex; align-items:flex-start; gap:8px;
  padding-top:10px; border-top:1px solid var(--border);
}
.scripture-icon { font-size:11px; color:var(--green); flex-shrink:0; margin-top:1px; }
.field-scripture {
  flex:1; background:transparent; border:none; outline:none;
  color:var(--green); font-size:13px; font-weight:400; font-family:var(--font);
  line-height:1.6;
}
.section-divider { height:1px; background:var(--border); }

/* LOG */
.btn-add-log { font-size:12px; color:var(--accent); font-weight:600; }
.new-log-form {
  background:var(--bg-input); border:1px solid var(--border);
  border-radius:var(--radius); padding:14px;
  display:flex; flex-direction:column; gap:12px;
}
.new-log-form.hidden { display:none; }
.log-entry {
  background:var(--bg-card); border:1px solid var(--border);
  border-radius:var(--radius-sm); padding:12px 14px;
  display:flex; flex-direction:column; gap:5px;
}
.log-entry-date { font-size:10px; font-weight:700; color:var(--ink-3); letter-spacing:0.06em; text-transform:uppercase; }
.log-entry-content { font-size:13px; color:var(--ink-1); line-height:1.75; font-weight:400; }
.log-entry-scripture { font-size:12px; color:var(--green); line-height:1.55; }
#prayer-log-list { display:flex; flex-direction:column; gap:8px; }
.btn-primary {
  background:var(--accent); color:#FBF9F6; border-radius:var(--radius-sm);
  padding:12px 18px; font-size:14px; font-weight:600;
  font-family:var(--fontb); transition:opacity 0.15s; width:100%;
}
.btn-primary.sm { padding:9px 14px; font-size:12px; width:auto; }
.btn-primary:active { opacity:0.85; }
.btn-delete {
  width:100%; padding:12px; color:var(--red); font-size:13px; font-weight:500;
  border:1px solid rgba(158,62,48,0.2); border-radius:var(--radius-sm);
  background:transparent; transition:background 0.15s;
}
.btn-delete:active { background:rgba(158,62,48,0.06); }

/* SETTINGS */
.side-panel {
  position:fixed; top:0; right:0; bottom:0; width:min(300px,86vw);
  background:var(--bg-card); border-left:1px solid var(--border);
  z-index:200; display:flex; flex-direction:column;
  transform:translateX(100%); transition:transform 0.28s cubic-bezier(0.32,0.72,0,1);
  box-shadow:-2px 0 16px rgba(30,26,23,0.08);
}
.side-panel.open { transform:translateX(0); }
.panel-overlay { position:fixed; inset:0; z-index:199; background:rgba(30,26,23,0.2); }
.panel-overlay.hidden { display:none; }
.panel-header {
  display:flex; align-items:center; justify-content:space-between;
  padding:18px 18px 14px; border-bottom:1px solid var(--border);
}
.panel-header h3 { font-family:var(--fontb); font-size:16px; font-weight:700; color:var(--ink-1); }
.panel-body { flex:1; overflow-y:auto; padding:20px 18px; display:flex; flex-direction:column; }
.settings-section { margin-bottom:28px; }
.settings-label {
  font-size:10px; font-weight:700; color:var(--ink-3);
  letter-spacing:0.1em; text-transform:uppercase; margin-bottom:10px;
}
.btn-settings-item {
  display:flex; align-items:center; gap:9px; width:100%; padding:12px 13px;
  background:var(--bg-input); border:1px solid var(--border);
  border-radius:var(--radius-sm); color:var(--ink-1); font-size:13px;
  font-weight:500; margin-bottom:7px; cursor:pointer; transition:background 0.15s;
}
.btn-settings-item:active { background:var(--bg-tag); }
.storage-info { font-size:12px; color:var(--ink-2); line-height:1.75; }

/* TOAST */
.toast {
  position:fixed; bottom:30px; left:50%; transform:translateX(-50%);
  background:var(--ink-1); color:#FBF9F6; border-radius:100px;
  padding:9px 22px; font-size:12px; font-weight:500;
  z-index:999; white-space:nowrap;
  box-shadow:0 3px 14px rgba(30,26,23,0.2);
}
.toast.hidden { display:none; }
.hidden { display:none !important; }

@media (min-width:600px) {
  .modal-sheet { border-radius:20px; margin:auto auto 0; }
  #screen-app { max-width:520px; margin:0 auto; border-left:1px solid var(--border); border-right:1px solid var(--border); }
}

.tab-emoji { font-size: 16px; line-height: 1; }

.modal-title-label { font-size: 13px; font-weight: 600; color: var(--ink-2); }

/* 달력 시작 뱃지 */
.cal-start-badge {
  display: inline-block;
  font-size: 10px;
  font-weight: 600;
  color: var(--ink-3);
  background: var(--bg-tag);
  border: 1px solid var(--border);
  border-radius: 100px;
  padding: 1px 7px;
  margin-left: 4px;
  vertical-align: middle;
  letter-spacing: 0.03em;
}

/* 달력 디테일 행간 */
.cal-detail-item-title {
  font-size: 14px;
  font-weight: 600;
  color: var(--ink-1);
  margin-bottom: 5px;
  line-height: 1.55;
}
.cal-detail-item-content {
  font-size: 13px;
  color: var(--ink-2);
  line-height: 1.75;
}
